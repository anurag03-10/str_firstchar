#include<stdio.h>
void main()
{
 int i=0;
 char a[50];
 printf("Enter a string");
 fgets(a,50,stdin);  
 printf("First characters of the string are");
 for(;a[i]!='\0';i++)
 {
  if(i==0&&a[i]!=32) 
  printf("%c ",a[i]);
  else if(a[i]==32)
  printf("%c ",a[i+1]);
 } 
}
